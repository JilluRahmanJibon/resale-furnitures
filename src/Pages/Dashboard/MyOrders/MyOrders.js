import React from 'react';
import useTitle from '../../../Hooks/useTitle';

const MyOrders = () => {
    useTitle("My Orders ")
    return (
        <div>
            here is my orders
        </div>
    );
};

export default MyOrders;